name: Ping Backend

# Déclencheurs
on:
  schedule:
    - cron: '*/1 * * * *'  # toutes les 15 minutes (UTC)
  workflow_dispatch:         # permet de lancer manuellement

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -s https://keskonmange.onrender.com/ping